security{
  rules = [
    {
      method = GET
      path = "/books"
      authentication = Secured
      authorization = [user]
      authorizationOp = AND
    },
    {
      method = GET
      path = "/books/\\d*"
      authentication = UserAware
    }
  ]
}