# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Map static resources from the /public folder to the /assets URL path

POST    /signup                 controllers.auth.AuthenticationController.signUp(redirect:String)
POST    /confirm/:token         controllers.auth.AuthenticationController.confirmAccount(token:String, redirect:String)

POST    /signin                 controllers.auth.AuthenticationController.signIn(redirect:String)

POST    /signout                controllers.auth.AuthenticationController.signOut(redirect:String)

POST    /reset                  controllers.auth.AuthenticationController.sendResetPassword(redirect:String)

POST    /reset/:token           controllers.auth.AuthenticationController.resetPassword(token:String)

GET     /index                  controllers.auth.AuthenticationController.index